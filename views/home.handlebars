{{!-- res.render('all') will get this file name in the controller directory route file that you set --}}
{{!-- the render function takes in 2 parameters: 1. handlebar template name 2. object full of options { example: dishes }  --}}
{{!-- if object variable name & property name and value are the same you dont need dishes : dishes -if you use eslint it will yell at you to include short hand notation syntax --}}

{{!-- you will need helpers: --}}
{{!-- there are 2 helpers: #if or #each looping statements known as conditional and looping which are built in --}}
{{!-- 6 built in  helpers: https://handlebarsjs.com/guide/builtin-helpers.html --}}
{{#each ___}}
{{/each }}
